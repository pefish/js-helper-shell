{
  "name": "@pefish/js-helper-shell",
  "version": "0.1.2",
  "types": "./types/shell.d.ts",
  "main": "./lib/shell.js",
  "dependencies": {
    "shelljs": "^0.8.5"
  },
  "scripts": {
    "test": "mocha --require ts-node/register src/*.test.ts",
    "build": "rm -rf lib types && tsc",
    "prepublish": "yarn build"
  },
  "devDependencies": {
    "@types/chai": "^4.1.7",
    "@types/mocha": "^5.2.6",
    "@types/node": "^11.13.7",
    "mocha": "^9.2.2",
    "ts-node": "^8.1.0",
    "typescript": "^3.4.5"
  }
}
